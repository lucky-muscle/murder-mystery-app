実行方法

1. Node.js がインストールされている環境で、この bin フォルダを開く。
2. run.bat をダブルクリックする。
3. ブラウザで http://localhost:4173 を開く。

補足：
本作品はWebアプリケーションのため、実行可能形式としてビルド済みのHTML/CSS/JavaScriptファイルを bin/web に収めています。
Reactなどの依存ライブラリはビルド時にJavaScriptファイルへまとめられているため、別途 node_modules を同梱する必要はありません。